-- learn.hs

module Learn where

-- First declare the name of our module so it
-- can be imported by name in a project.
-- We won't do this for a while yet.

-- since Haskell loads the entire file, y can be declared at a later point without compilation error
x = 10 * 5 + y

myResult = x + 5

y = 10
